// file for product

const Review = async () => {
  await new Promise((resolve) => setTimeout(resolve, 4000));
  return (
    <div>
      <h1>review page</h1>
    </div>
  );
};

export default Review;
